# AgentForge Frontend — environment config
# Vite exposes VITE_* vars to the browser via import.meta.env
# Copy to .env.local to override without touching this file

VITE_BACKEND_HOST=localhost
VITE_BACKEND_PORT=9000
VITE_FRONTEND_PORT=5173
